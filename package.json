{
  "name": "mlir-viz",
  "version": "0.2.0",
  "private": false,
  "type": "module",
  "scripts": {
    "wasm": "cd wasm && bash build.sh",
    "dev": "npm run wasm && vite",
    "build": "npm run wasm && vite build",
    "preview": "vite preview",
    "clean": "rm -rf dist public/mlir_core.wasm wasm/target"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}
